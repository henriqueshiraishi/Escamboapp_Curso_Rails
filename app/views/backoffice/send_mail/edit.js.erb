$(function(){
  let $sendMailModal = $('#sendMailModal')


  $('#recipient-text').val('<%= @admin.email %>');
  $('h4.modal-title').text('<%= t('message.send_email_to') %> <%= @admin.name %>');

  $sendMailModal.modal();

  $sendMailModal.on('show.bs.modal', function () {
     $('#subject-name').focus();
  })
});
